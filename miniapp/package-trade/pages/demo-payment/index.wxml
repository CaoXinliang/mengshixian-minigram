<view class="page"><view class="topbar"><text bindtap="back">‹</text><view><text>支付确认</text><text>测试环境 · 不发生真实扣款</text></view></view><view wx:if="{{loading}}" class="state">正在读取已锁定订单…</view><view wx:elif="{{failed}}" class="state"><text>{{errorText}}</text><button bindtap="retry">重新读取订单</button><button bindtap="back">返回订单页</button></view><block wx:else><view class="hero"><text>支付确认（测试）</text><text>订单已锁定</text><text>订单号 {{order.orderNo}}</text><text wx:if="{{order.itemSummary}}" class="item-summary">{{order.itemSummary}}</text><text>应付合计 ¥{{order.total}}</text></view><view class="note"><text>测试环境，不会扣款</text><text>不会唤起微信支付，也不会产生真实支付凭证。</text></view><view class="bottom"><button bindtap="finish">完成支付确认</button></view></block></view>
