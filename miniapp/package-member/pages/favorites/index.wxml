<view class="page"><responsive-topbar title="我的收藏" subtitle="顾客与企业采购账户均可使用" bind:back="back"/><view wx:if="{{status == 'loading'}}" class="state">正在加载收藏</view><view wx:elif="{{status == 'forbidden'}}" class="state"><text>需要登录</text><text>{{errorText}}</text></view><view wx:elif="{{status == 'error'}}" class="state error"><text>收藏加载失败</text><text>{{errorText}}</text><button bindtap="retry">重新加载</button></view><view wx:elif="{{status == 'empty'}}" class="state"><text>暂无收藏商品</text><text>在商品详情可添加收藏</text></view><view wx:else class="list"><view wx:for="{{rows}}" wx:key="id" class="card"><image src="{{item.image}}" mode="aspectFit"/><view bindtap="openProduct" data-id="{{item.id}}"><text>{{item.name}}</text><text>{{item.spec}}</text><text wx:if="{{item.unavailable}}">商品当前不可购买</text></view><button disabled="{{busyId}}" bindtap="remove" data-id="{{item.id}}">{{busyId == item.id ? '处理中' : '取消收藏'}}</button></view></view></view>
